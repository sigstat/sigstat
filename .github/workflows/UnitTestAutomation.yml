name: UnitTestAutomation

on: 
  push:
    branches : [codacy_refactor]
  pull_request:
    bracnhes : [master]


jobs:
  build:
    runs-on: windows-latest
    steps:
      -uses: actions/checkout@v2.1.0
      -run: dotnet test

